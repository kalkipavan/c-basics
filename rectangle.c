#include<stdio.h>
void main()
{
	int l,w,a,p;
	printf("enter l,w");
	scanf("%d%d",&l,&w);
	a=l*w;
	p=2*(l+w);
	printf("area pf the rectangle %d\n",a);
	printf("perimeter of the rectangle %d",p);
}
